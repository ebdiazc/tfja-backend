[variables]
PYTHON_VERSION = "3.11"
```
*(Solo esa línea — Railway detectará el Dockerfile automáticamente y usará eso)*

---

### 3. Editar `Procfile` — reemplazar todo con esto:
```
web: gunicorn app:app --timeout 180 --workers 1
